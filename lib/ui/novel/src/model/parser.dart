import 'scenario.dart';

/// Parses the provided [String]s into the [NovelLine]s.
class NovelParser {
  const NovelParser();

  /// Returns the parsed [NovelLine] from the provided [String].
  ScenarioLine? parse(String? line) {
    return null;
  }
}
